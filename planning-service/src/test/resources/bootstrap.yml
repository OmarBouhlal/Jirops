# planning-service/src/test/resources/bootstrap.yml
spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
      fail-fast: false
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
